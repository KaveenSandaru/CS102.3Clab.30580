#include <stdio.h>
int main()
{
    int in,sum;
  while(in>=-1)
{
    printf("Enter a number : ");
    scanf("%d",&in);
    sum=sum+in
}
    printf("The sum of these number is %d”,sum);
   return 0;
}
